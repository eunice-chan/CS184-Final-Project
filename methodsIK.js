function DLS( param ) {
	var lambda = Math.max(...diagJTJ);
	var beta = param.beta
	// for ( int k = 0; k < param.k_max; k ++ ) {
	//
	//
	// }

	// manipulate mesh. when solution found, set
	// var position = geometry.attributes.position;
  // position.needsUpdate = true;
}

function PIDLS( param ) {
	console.log("PIDLS");
}

function SMCM( param ) {
	console.log("SMCM");
}
